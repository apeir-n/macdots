# alias ls = eza --icons --group-directories-first --color=auto --oneline --all
# alias ll = ls -lah
# alias la = ls -A
# alias lt = ls -Tlh
# alias grep = grep --color=auto
# alias wget = wget --hsts-file=$env.XDG_DATA_HOME/wget-hsts
# alias man = batman
# alias news = hackernews_tui
# # alias colors = cat $env.XDG_CACHE_HOME/wal/colors | pastel format
# alias stars = astroterm -Cc -s 400 -a 41.1081 -o -81.5148
# alias stors = astroterm -Cuc -s 400 -a 41.1081 -o -81.5148
# alias cr = cargo run -q
# alias ff = fastfetch
# alias xini = $env.EDITOR $HOME/.xinitrc
# alias xres = ${EDITOR} $HOME/.Xresources
# alias beget = sudo make clean install && make clean
# alias pa = ping apple.com
# alias pkglist = brew list --installed-on-request | mmenu
# alias paper = $HOME/.local/bin/paper
# alias rust = rustc --edition 2024 -C strip=symbols -C opt-level=s -C panic=abort
# alias skhd = $HOME/.local/scripts/skhd
# alias preview = wal --preview | sed 1d
# alias pat = bat -pp
# alias ttmux = tmux new -s tmux
# alias ttiny = tmux new -s tiny
# alias nnn = nnn -P p
# alias mpcr = mpc play && mpc pause

# alias o = cd $VAULT && y
# alias op = cd $VAULT/planner && y
# alias pass = v misc _sensitiveinfo
# alias wp = cd $HOME/Pictures/wallpapers && y
# alias scripts = cd $HOME/.local/scripts && y
# alias pc = cd $HOME/.cache/wal && y
# alias baby = cd $HOME/.baby && y
# alias suck = cd $HOME/.local/src/suckless && y
# alias src = cd $HOME/.local/src && y
# alias lib = cd $HOME/.local/lib && y
# alias lang = cd $HOME/.local/opt/_lang && y
# alias code = cd $HOME/Projects && y
# alias comfy = cd $HOME/.local/ai/ComfyUI/output && y
# alias dl = cd $HOME/Downloads && y
# alias sdk = cd /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk && y
